def train_baseline(X, y, model_name="rf"):
    """
    TODO: fit a model and return it.
    X: features (numpy array)
    y: labels (numpy array)
    model_name: "rf" or "logreg"
    """
    return None

def evaluate_model(model, X, y):
    """
    TODO: return metrics (AUC, confusion matrix...)
    """
    return {"auc": None, "cm": None}